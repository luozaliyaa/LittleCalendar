# AGENTS.md

本文件是仓库级开发说明，适用于 Codex、其他代码 Agent 和人工贡献者。

## 项目边界

LittleCalendar 是独立的 Windows WPF 本地应用。它不依赖同级目录中的 Edge 扩展，也不应引入服务器、云数据库、遥测或用户账号系统。

除非需求明确改变安全边界，否则不得：

- 自动发送、删除或移动邮件；
- 自动提交招聘表单、上传附件或处理验证码；
- 把日历数据、邮箱授权码或 API Key 上传到仓库或第三方日志；
- 在后台执行用户未启用的邮箱读取或模型请求。

## 模块职责

- `Program.cs`：入口、单实例和启动参数。
- `Desktop.cs`：主窗口、编辑窗口、设置、托盘和桌面提醒。
- `CalendarVisuals.cs`：月历待办气泡和跨日期期限条。
- `Model.cs`：持久化模型、迁移、备份与回收站。
- `Deadlines.cs`、`DeadlineFields.cs`：期限语义、计算和校验。
- `Agent.cs`：DeepSeek 日历总结，不得写入或修改待办。
- `MailClient.cs`：IMAP 连接、读取和允许的邮件标记操作。
- `MailAnalysis.cs`：邮件清理、提示词、JSON Schema 和本地校验。
- `MailSync.cs`：同步编排、分流、去重、创建待办和完成回写。
- `MailStore.cs`：非敏感同步状态。
- `MailDiagnostics.cs`：脱敏、限长和轮转日志。
- `Tests.cs`：自动化测试入口。

## 开发流程

功能或缺陷修改使用测试驱动流程：先添加会失败的测试，确认失败原因正确，再做最小实现，最后运行完整测试。

```powershell
.\restore-packages.ps1
.\build.ps1 -Test -OutputDirectory test-output
```

不要用真实邮箱、授权码、DeepSeek Key、真实 Message-ID 或真实招聘邮件做测试。所有测试必须使用明显虚构的数据，并通过 `--data-dir` 或测试工具提供的临时路径隔离，禁止访问日常的 `%LOCALAPPDATA%\LittleCalendar`。

## 必须保持的行为

- 旧数据迁移必须向后兼容，并在破坏性升级前保留原始备份。
- 日历与同步状态应采用临时文件加替换的方式保存，避免异常退出产生半截 JSON。
- “邮件发送后 N 小时/天”从邮件原始时间和时区计算，不从扫描时间计算。
- 模糊的“两天内”等期限保持“待确认”，不能伪装成已核实的精确截止时间。
- 邮件先按 Message-ID/UID 去重，再做语义去重；相同任务的催办应合并，不同场次或截止日期不得误合并。
- 已完成的邮件任务不得被后续催办重新创建。
- 邮件分类只允许 `todo`、`notice`、`ignore`：真正需要行动的事项进入日历，机会宣传进入通知中心，无关内容忽略。
- 创建邮件待办只允许加星；待办完成后只允许已读和取消星标。不得发送、删除或移动邮件。
- WPF 后台任务更新界面必须通过 `Dispatcher` 回到 UI 线程。
- 日历智能总结是只读功能，不得借模型输出自动改写用户数据。

## 不可信输入与安全

邮件正文、HTML、附件名和模型返回值全部是不可信输入：

- 清理 HTML，不执行脚本，不加载远程资源，不读取普通附件正文。
- 模型请求必须使用固定 JSON Schema；解析后继续做枚举、长度、日期、时区和业务规则校验。
- 邮件中的“忽略规则”“泄露密钥”“执行命令”等内容只能作为邮件文本，不能改变系统提示或程序权限。
- 日志只记录排障所需的阶段和脱敏元数据，不记录密钥、授权码、完整正文、完整提示词或模型原始输出。
- 新增日志字段和导出字段时先检查是否可能包含个人信息。

## 代码与界面约定

- 保持 .NET Framework 4.6.2 兼容，除非单独完成并说明运行时升级。
- 遵循现有 C# 风格，优先小而清楚的改动，不为局部功能引入大型框架。
- 中文界面文案使用用户能理解的表达；错误信息应说明下一步，不显示密钥或原始协议载荷。
- 不提交 `release*`、`test-*`、`.packages*`、日志、用户数据、EXE、DLL 或 ZIP。

## 提交前检查

1. 完整构建和测试通过。
2. 新功能包含成功、边界、失败和安全场景测试。
3. 用隔离数据目录做一次启动冒烟测试。
4. 运行 `scripts/Test-RepositoryHygiene.ps1`，确认没有密钥、个人数据或生成物。
5. 运行 `git diff --check`，检查提交内容没有无关文件。
6. 若修改发布行为，用 `scripts/New-ReleasePackage.ps1` 生成并检查 ZIP 内容。
